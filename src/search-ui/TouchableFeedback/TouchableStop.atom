<template>
    <div :rl-type="open ? 'stop' : null">
        <!-- @slot 默认 slot -->
        <slot/>
    </div>
</template>

<script type="config">
{
    props: {
        /**
         * 是否添加点击态
         */
        open: {
            type: Boolean,
            default: true
        }
    }
}
</script>